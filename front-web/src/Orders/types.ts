export type Product = {
    id: number;
    name: string;
    price: number;
    description:string;
    imgUri:string;
}

export type OrderLocationData = {
    latitude:number,
    longitude:number,
    address:string
}
